# 联软渠道管理平台 v2.2.0 — 项目检查报告

> 检查时间：2026-04-12  
> 检查人：接手人（首次接手）  
> 状态：待迁移至 Linux 服务器

---

## 一、项目概况

| 项目 | 内容 |
|------|------|
| 系统名称 | 联软安全渠道管理平台 |
| 当前版本 | v2.2.0（2026-04-12） |
| 用途 | 管理渠道伙伴、商机、报价单、订单的 B2B CRM 系统 |
| 用户角色 | 超级管理员 / 区域管理员 / 渠道员工（三级权限） |

---

## 二、技术架构

```
┌─────────────────────────────────────────┐
│  前端（端口 8080）                        │
│  Vue 3 + Element Plus（CDN，无需编译）    │
│  纯 HTML/JS 静态文件，Python3 HTTP 服务  │
└──────────────┬──────────────────────────┘
               │ REST API
┌──────────────▼──────────────────────────┐
│  后端（端口 3000）                        │
│  Node.js + Express v5                   │
│  数据存储：backend/data.json（内存+文件） │
└─────────────────────────────────────────┘
```

- **前端**：Vue3 + ElementPlus（均走 CDN，无构建步骤），静态文件
- **后端**：Node.js + Express，依赖仅 `express`、`cors` 两个包，极简
- **数据层**：内存存储 + `data.json` 文件持久化（**无数据库**）

---

## 三、目录结构

```
lianruan-crm-deploy-v2.2.0/
├── frontend/               # 前端静态页面
│   ├── login.html          # 统一入口（设备自动适配）
│   ├── admin.html          # 管理后台
│   ├── partner.html        # 渠道伙伴（PC）
│   ├── partner-mobile.html # 渠道伙伴（手机）
│   ├── api-client.js       # 前后端对接层
│   └── libs/               # 本地 CDN 备份文件
├── backend/                # 后端服务
│   ├── server.js           # 主服务
│   ├── data.json           # ⚠️ 核心数据文件，必须备份！
│   └── package.json
├── scripts/
│   ├── start.sh            # 日常运维脚本（推荐使用）
│   └── deploy.sh           # 自动部署脚本（需 root）
├── install.sh              # 一键安装脚本
├── docs/                   # 文档目录
└── 项目检查报告.md          # 本文件
```

---

## 四、Linux 部署要求

| 依赖 | 最低版本 | 说明 |
|------|---------|------|
| Node.js | ≥ 16.x | 运行后端 |
| npm | 随 Node.js | 安装依赖 |
| Python3 | ≥ 3.6 | 运行前端静态服务（可选，也可用 Nginx） |
| 内存 | ≥ 512MB | |
| 磁盘 | ≥ 1GB | |

---

## 五、⚠️ 发现的问题

### 🔴 问题1（已修复）：`scripts/start.sh` 目录路径错误

`start.sh` 第17行原为 `backend-simple`，已修正为 `backend`。  
**状态：✅ 已修复**

---

### 🔴 问题1b（已修复）：`scripts/start.sh` 路径定义 + 前端启动双重修复

共3处修复：

1. **路径问题**：新增 `PROJECT_DIR="$(dirname "$SCRIPT_DIR")"`，让 `BACKEND_DIR`、`FRONTEND_DIR` 正确指向项目根目录而非 `scripts/` 子目录
2. **前端无失败检测**：原 `start_frontend` 不检查进程是否存活就报成功，已添加 `kill -0` 检测
3. **前端只绑定本地**：`python3 -m http.server` 默认只监听 `127.0.0.1`，局域网其他机器无法访问，已加 `--bind 0.0.0.0`

**状态：✅ 已修复**

---

### 🔴 问题2（已修复）：`scripts/deploy.sh` 缺少 backend 和 scripts

原 deploy.sh 只复制了 frontend，遗漏了 backend 目录和脚本文件。已修正。  
**状态：✅ 已修复**

---

### 🔴 问题3（已修复）：`frontend/index.html` 跳转地址错误

第23行原跳转到 `mobile.html`，已修正为 `partner-mobile.html`。  
**状态：✅ 已修复**

---

### 🟡 问题4（已修复）：`docs/README-DEPLOY.md` 目录结构错误

文档中目录结构标注为 `backend-simple`，已修正为 `backend`。  
**状态：✅ 已修复**

---

### 🟡 问题5（已修复）：`frontend/libs/` 测试文件残留

已删除以下开发遗留文件：
- `test-binary.js`
- `test-extract.js`
- `test-find.js`
- `test-problem.js`
- `test-vue-internal.js`
- `test-vue.js`
- `vue.min.js.bak`

**状态：✅ 已清理**

---

### 🟡 问题6：数据安全风险（建议）

- `backend/data.json` 包含所有业务数据，文件丢失则**数据全部消失**
- 无数据库，无冗余备份，仅靠单文件持久化
- 默认密码均为 `123456`，**生产环境使用前必须修改**

**建议：**
```bash
# 定期备份 data.json（示例：每天凌晨2点备份）
0 2 * * * cp /opt/lianruan/backend/data.json /opt/backup/data_$(date +\%Y\%m\%d).json
```

---

### 🟡 问题7：前端 API 地址硬依赖 3000 端口

`api-client.js` 会自动拼接 `http://host:3000/api`。  
若将来使用 Nginx 反向代理统一走 80/443 端口，需在每个 HTML 文件 `<head>` 中加一行：

```html
<script>window.API_BASE = '/api';</script>
```

---

## 六、Linux 部署步骤（推荐）

```bash
# 1. 上传压缩包到服务器
scp lianruan-crm-deploy-v2.2.0.tar.gz user@your-server:/opt/

# 2. 解压
cd /opt && tar -xzf lianruan-crm-deploy-v2.2.0.tar.gz
cd lianruan-crm-deploy-v2.2.0

# 3. 安装依赖并启动服务（所有 Bug 已修复，可直接运行）
bash scripts/start.sh install

# 4. 开放防火墙端口
sudo ufw allow 8080/tcp
sudo ufw allow 3000/tcp

# 5. 验证服务状态
bash scripts/start.sh status
```

**访问地址：** `http://服务器IP:8080/login.html`

---

## 七、日常运维命令

```bash
bash scripts/start.sh start    # 启动
bash scripts/start.sh stop     # 停止
bash scripts/start.sh restart  # 重启
bash scripts/start.sh status   # 查看状态
bash scripts/start.sh logs     # 查看日志
```

---

## 八、总结评估

| 维度 | 评估 |
|------|------|
| 部署难度 | ⭐ 低（无数据库，无构建，脚本齐全） |
| 数据风险 | ⚠️ 中（单文件存储，务必定期备份） |
| 迁移复杂度 | ⭐ 低（依赖极少，Node.js + Python3 即可） |
| 代码质量 | ✅ 良好（结构清晰，注释完整） |
| 遗留文件清理 | ✅ 7个测试文件已删除 |
| 所有问题修复 | ✅ 全部4个代码/脚本 Bug 已修复 |

**结论：** 所有代码级 Bug 已全部修复，项目可安全迁移至 Linux 服务器。**核心风险是数据备份，请务必建立定期备份机制。**

---

*本报告由 AI 助手自动生成，基于项目文件分析，如有出入请以实际代码为准。*
