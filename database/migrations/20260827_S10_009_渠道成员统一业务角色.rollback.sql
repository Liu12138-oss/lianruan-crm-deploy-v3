-- S10-009 安全回退：统一成员资料通过功能开关和应用版本回退。
-- 不删除新增列，不停用或删除销售/技术角色，不撤销证书，也不反转已经发生的归档事实。
-- 迁移无法可靠区分“本次新建角色”和“迁移前已存在角色”，因此禁止在回退中静默改变正式角色状态。

DO $$
BEGIN
  RAISE NOTICE 'S10-009 采用保留数据回退；请关闭组织写入与入口合并开关，并回退应用版本。';
END $$;
