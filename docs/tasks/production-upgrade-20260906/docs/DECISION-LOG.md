# 决策日志

## 2026-09-06

### 已确认

- 用户确认将当前本地全部已确认改动冻结为 `3.0.0-stage10.20260906-prod1`。
- 生产升级来源以实际运行 `3.0.0-stage9.40.20260813-kb022` 及其三类镜像摘要为准，不以漂移的 `compose/.env` 标签为准。
- 用户允许今日最多三小时维护窗口，并要求升级不丢失、不覆盖生产业务数据。
- 渠道商地市区号保持为空，不按名称、手机号或外网资料猜测。
- 仅启用泛微 OA 订单预审；企微应用消息和邮件外发暂不启用。
- 泛微 OA 正式参数由生产管理员自行通过安全渠道配置，禁止提交至仓库或升级包。
- 升级前先在隔离数据库副本演练。

### 采用方案

使用 `SP-APP` 完整应用升级包；先执行源版本专用预检查和数据迁移验证，失败时恢复应用快照，数据库不自动恢复。

### 否决方案

- 不按 `kb026` 配置标签重启或直接切换：Worker 镜像缺失，且实际容器仍为 `kb022`。
- 不在升级时直接执行未验证的所有历史迁移：生产迁移谱系存在跳跃。
- 不自动填充地市区号：生产只有城市名称，无法可靠推断标准区号。
- 不把 OA、企微应用或邮件密钥写入代码、文档、包或日志。
