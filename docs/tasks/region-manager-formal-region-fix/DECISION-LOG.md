# 决策记录

## 2026-09-09

### 已确认事实

- `chenqinyu` 是启用的 `region_manager`，扩展信息区域为“湖南区”，`iam.users.region_id` 为空。
- 当前订单查询按渠道商区域可见，但订单确认要求区管正式区域编号与渠道商区域编号相等。
- 生产另有 `liuchengzhou`、`yucaihong` 存在同类缺失。

### 用户确认

- 修复共性问题，完成本地验证和测试，最后更新生产版本。

### 采用方案

- 账号保存时同步正式区域编号。
- 存量数据仅对精确唯一匹配的启用区管自动补齐。
- 保留订单确认的严格正式区域校验，避免跨区授权。

### 未采用方案

- 不使用展示区域名称直接放宽订单确认权限。
- 不批量覆盖已有非空正式区域。
- 不修改订单状态或人工代替区管确认。
