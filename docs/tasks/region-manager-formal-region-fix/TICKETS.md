# 任务单

## RM-001 账号保存同步正式区域

- 修改 `apps/api/src/v2-compat-routes.ts`。
- 增加精确、唯一、有效区域解析。
- 区域管理员创建和编辑写入 `iam.users.region_id`。
- 验收：账号保存后正式区域可用于订单确认。

## RM-002 存量账号安全补齐

- 新增 `database/migrations/20260909_S10_021_区管正式区域绑定修复.sql` 及回退脚本。
- 仅处理启用区管且扩展区域名称精确匹配唯一有效区域的账号。
- 记录迁移标识和前后值，回退只针对本次标识。

## RM-003 自动化测试与发布验证

- 补充 V2 账号保存和正式区域编号测试。
- 执行 API 测试、构建、迁移检查和生产发布前备份。
- 生产验证账号、订单状态、跨区拒绝和健康版本。
