# 订单预审退回重提后可重试任务单

## OPR-01：审批轮次数据迁移

- 状态：已完成。
- 增加 `approval_round` 字段并回填存量请求；将唯一约束调整为订单＋轮次；补充正向和安全回退 SQL。
- 验收：迁移可重复执行，存量请求可读，新轮次可插入，存在新轮次事实时回退明确阻断。

## OPR-02：状态机创建请求按轮次幂等

- 状态：已完成。
- 更新 API 预审请求创建、轮次计算、历史结果门禁、幂等键和审计；不改变订单状态机触发条件。
- 验收：失败重提可创建新轮次，成功/未知结果不重发，同轮并发不重复。

## OPR-03：请求快照和对账展示

- 状态：已完成。
- 将轮次写入请求快照、审计和必要的查询结果，保证 Worker 使用新请求的当前报价快照。
- 验收：旧请求和新请求可按轮次区分，历史调用、附件和外部编号不被覆盖。

## OPR-04：测试与回归

- 状态：已完成。
- 补充 API 集成测试、Worker 查询/状态测试和迁移静态检查；执行 V3 全量构建、类型检查和主业务回归。
- 验收：本任务新增用例全部通过，既有报备、商机、报价、订单、渠道、账号和 Worker 测试无回归。
