# 决策记录

## 2026-09-06：身份来源与生产写入边界

- 用户确认《联软账号去敏版本（企微对应账号）.xlsx》是企微映射全量来源，并同意按全量匹配现有内部人员的策略生成生产导入脚本。
- 采用“Excel 企业微信账号 + 生产泛微参考表数字 userid”双来源，禁止混用两个外部系统的编号。
- 用户确认 Excel 的“姓名、账号”是全量权威映射来源。因此对于历史 V3 登录名与外部账号不一致的情况，只要 Excel 与泛微参考表的姓名、账号均唯一且一致，允许在预检清单中作为同一人的受控映射候选；仍不按区域名或泛化管理员显示名猜测。
- 采用默认预检、阻断项拒绝写入、显式确认、超级管理员审计的方式，避免部分映射导致自动 OA 发起和自动拉群出现不可预测缺口。

## 2026-09-07：排除待清理账号

- 用户确认本次不处理预检中缺少映射的 14 个区域管理员，以及 `region_admin_save_1788232088689_login`、`wangbo`、`admin` 这 3 个内部账号。
- 随后用户确认 `admin_zhy` 也不参与导入，并要求按 V3 发起账号保留 `A032`、`A029`，因此 `yucaihong`、`liuchengzhou` 也排除；显式排除名单合计 20 个登录名。排除仅适用于本次身份映射导入，不会停用、删除或修改这些生产账号。

## 2026-09-07：湛怀玉账号保留规则

- 用户确认 `admin_zhy` 为待删除账号，本次排除。
- 用户确认保留 `zhanhuaiyu`，并为其同时导入企业微信与泛微 OA 正式身份；该账号不因当前不是 OA 发起人而跳过泛微映射。
