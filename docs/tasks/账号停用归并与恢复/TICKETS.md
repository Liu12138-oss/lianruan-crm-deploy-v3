# 任务单

## T1：角色成员停用账号展示收口（已完成）

- 默认过滤停用账号，支持显式查看历史。
- 验收：停用账号不计入默认使用人数。

## T2：账号恢复并重新授权（已完成）

- 建立服务端状态校验、角色覆盖、新任职和审计。
- 验收：只允许完成交接的停用账号恢复，旧会话不恢复。

## T3：重复账号归并（已完成）

- 建立来源、保留账号和交接影响预览；复用既有交接流程。
- 验收：不允许物理删除、不允许自动按姓名归并。
