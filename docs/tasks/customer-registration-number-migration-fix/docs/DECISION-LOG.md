# 客户报备业务编号迁移覆盖缺陷修复决策记录

## 共识摘要

- 用户授权在本地修复并完成测试验证，不修改生产环境。
- 根因已由生产只读排查确认：历史迁移的补执行顺序覆盖了已正确执行的客户报备编号函数。
- 采用新的正向迁移修复，禁止改写已执行的历史迁移或直接修改生产数据库。

## 已确认决策

1. 修复对象仅为 `crm.next_business_number(text, text)` 的函数定义。
2. 客户报备继续按提报账号和自然日生成 `BB-账号-YYYYMMDD-日流水`，不改变业务状态、审批、权限或数据范围。
3. 以事务式集成测试模拟真实覆盖顺序；测试中产生的函数定义和计数器变动均在回滚后清理。
4. 已发现的用户未跟踪文件不属于本任务，保持原样。

## 否决方案

- 修改 `S9_020`、`S9_021`、`S9_022` 或 `S9_023`：会造成迁移摘要漂移，不能修复已发布环境。
- 直接在生产执行函数 SQL：缺少受控发布、校验与回退路径，不采用。

## 验证与回退

- 验证：迁移函数复现测试、客户报备主链路测试、API 类型检查、部署迁移顺序测试。
- 回退：如需撤回，只追加新的受控正向迁移恢复函数；不得删除 `schema_migrations` 记录或回滚业务数据。

## 本地验证记录

1. 本地测试库完整装载全部迁移成功，`20260907_S10_018_客户报备业务编号函数补齐.sql` 已在序列末尾执行。
2. 覆盖复现测试通过：旧 `S9_022` 函数先拒绝 `registration`，补齐迁移执行后四类业务编号均可生成。
3. 客户报备、商机、报价、订单第一闭环测试通过。
4. API 类型检查、格式检查、迁移顺序测试和差异空白检查通过。

## 生产发布记录

- 发布时间：2026-09-07 14:26（Asia/Shanghai）。
- 发布方式：离线热修复包摘要校验通过后，仅安装并执行 `20260907_S10_018_客户报备业务编号函数补齐.sql`；应用镜像、配置和容器未变更。
- 迁移摘要：`7618c5e061340378d37d063a81448b0356b709b800cdcbc4657e7a4d4112d630`。
- 发布前备份：`/home/lianruan-crm-v3-system-backups/20260907-142350-manual`，完整性校验通过。
- 发布后验证：函数白名单、`BB` 前缀和报备查重表均正确；事务内生成 `BB-release-verification-20260907-0001` 后已回滚；全部容器正常，`/health/live` 返回成功。
