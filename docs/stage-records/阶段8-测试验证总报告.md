# 阶段8测试验证总报告

生成日期：2026-07-27

## 1. 总体结论

阶段8.1、阶段8.2、阶段8.3已完成，并已在 HexHub 测试验证服务器“渠道CRM（新架构）”执行阶段8.3 PostgreSQL 暂存装载。

当前阶段8结论：

| 阶段    | 结论   | 说明                                                               |
| ------- | ------ | ------------------------------------------------------------------ |
| 阶段8.1 | 通过   | 已完成 V2 正式源库实体数量记录                                     |
| 阶段8.2 | 通过   | 已完成 V2 只读导出和脱敏检查                                       |
| 阶段8.3 | 通过   | 已完成 PostgreSQL `migration.*` 暂存装载包，并在测试服务器验收通过 |
| 阶段8.4 | 未开始 | 正式业务表迁移脚本和业务验收口径尚未完成                           |

本报告不能作为 V3 正式上线签收报告使用。

## 2. 阶段8.1源库记录

正式源库路径：

```text
/Users/liu/Documents/Codex/lianruan-crm-deploy-v2.2.0 3/lianruan-crm-deploy-v2.2.0 2/backend/crm.db
/Users/liu/Documents/Codex/lianruan-crm-deploy-v2.2.0 3/lianruan-crm-deploy-v2.2.0 2/backend/audit.db
```

统计结果：

| 项目         | 数量   |
| ------------ | ------ |
| CRM实体总数  | `698`  |
| 审计日志总数 | `393`  |
| 合计         | `1091` |

完整性结果：

| 源库       | 结果 |
| ---------- | ---- |
| `crm.db`   | `ok` |
| `audit.db` | `ok` |

## 3. 阶段8.2只读导出

导出目录：

```text
tmp/stage8/v2-export-official/
```

导出结果：

| 项目               | 结果        |
| ------------------ | ----------- |
| CRM导出记录        | `698 / 698` |
| 审计导出记录       | `393 / 393` |
| 导出文件数         | `20`        |
| 脱敏字段总数       | `122`       |
| SHA256校验         | 通过        |
| 结构化敏感字段扫描 | 通过        |

## 4. 阶段8.3暂存装载

本地装载包目录：

```text
tmp/stage8/v2-postgres-staging-official/
```

本地迁移补包：

```text
tmp/phase8-package-run-20260727/lianruan-crm-v3-stage8-staging-S8-RUN-20260727-001.zip
```

补包 SHA256：

```text
deabc0b23253893c77c3e00ab8f22b1afce7d00b3c69c5c23ac81f582387cac1
```

测试服务器装载结果：

| 项目         | 结果                  |
| ------------ | --------------------- |
| 批次编号     | `S8-RUN-20260727-001` |
| 批次状态     | `imported`            |
| 暂存总记录数 | `1091`                |
| 失败记录数   | `0`                   |
| 校验通过数   | `22`                  |
| 未解决异常数 | `0`                   |

## 5. 自动化验收记录

本地专项检查：

```bash
node tests/phase8/阶段8.1源库实体数量记录检查.js
node tests/phase8/阶段8.2只读导出检查.js
node tests/phase8/阶段8.3暂存装载包检查.js
```

结果：全部通过。

本地 SHA256 校验：

```bash
cd tmp/stage8/v2-postgres-staging-official
shasum -a 256 -c sha256sum.txt
```

结果：全部通过。

Node 22 完整门禁：

```bash
PATH="$PWD/.cache/nodejs/node-v22.13.1-darwin-arm64/bin:$PATH" npm run verify
```

结果：通过。

## 6. 测试服务器验证记录

测试服务器：

```text
10.20.3.10
```

健康检查：

| 项目                                           | 结果                    |
| ---------------------------------------------- | ----------------------- |
| `/opt/lianruan-crm-v3/scripts/health-check.sh` | 通过                    |
| `/health/live`                                 | 通过                    |
| `/health/ready`                                | 通过，状态为 `degraded` |
| `/health/dependencies`                         | 通过，状态为 `degraded` |

`degraded` 的原因是应用健康接口仍把正式数据库迁移标记为跳过，不是 PostgreSQL 或 Redis 连接失败。

浏览器入口验证：

| 路径               | 结果 |
| ------------------ | ---- |
| `/`                | 通过 |
| `/unified`         | 通过 |
| `/admin`           | 通过 |
| `/partner`         | 通过 |
| `/mobile`          | 通过 |
| `/session-expired` | 通过 |
| `/maintenance`     | 通过 |
| `/403`             | 通过 |
| `/not-exist-test`  | 通过 |

## 7. 阶段8收口判断

阶段8.1至阶段8.3已具备交接条件，可以作为阶段8.4正式业务表迁移设计和测试演练的输入。

阶段8.4不得直接开始正式落表，必须先完成前置阻断清单中的脚本、预校验、业务映射和验收口径。
