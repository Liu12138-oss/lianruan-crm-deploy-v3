# 阶段9.12 V2 UI严格还原纠偏验收报告

生成日期：2026-07-28  
负责人：产品交付总监  
工程目录：`/Users/liu/Documents/Codex/lianruan-crm-deploy-v3`  
V2基线目录：`/Users/liu/Documents/Codex/lianruan-crm-deploy-v2.2.0 2/frontend`  
专项目标：针对“V3 页面仍不像 V2”的反馈，继续做严格还原纠偏，不新增业务功能。

## 1. 交付结论

阶段9.12本地专项和 HexHub 远端发布验收均已完成。  
本次不是简单换色，而是把阶段9.11仍保留“统一通用业务壳”的关键页面，改成更接近 V2 的专属页面结构，并已发布到 HexHub「渠道CRM（新架构）」测试资产。

本轮已完成：

1. 商机管理补齐 V2 列表视图、看板视图、阶段列、金额合计。
2. 商机详情从通用详情网格改为 V2 左右分栏大弹窗。
3. 新建客户报备补齐 V2 步骤条、客户基础信息面板、企业查询提示。
4. 新建报价改为 V2 智能报价配置器，包含基本信息、报价方式、报价汇总三段。
5. 报价方式补齐套餐报价、补充功能、自定义报价三类卡片。
6. 产品选择改为卡片勾选，硬件加配区和报价汇总区按 V2 结构展示。
7. 手机端产品目录和新建报价补 `/mobile` 路由，避免从手机入口跳回桌面页面。
8. 手机列表写入入口确认跳转到 `/mobile/partner/registrations/new`，新建页隐藏底部导航。
9. Web 入口补齐移动端 `viewport-fit=cover`、禁止电话识别、主题色和苹果移动网页元信息。
10. E2E 从 9 个场景扩展到 13 个业务场景，并在桌面浏览器、移动浏览器两个项目下共执行 26 次，已纳入完整 `npm run verify`。

## 2. 本次修改范围

| 文件                                           | 说明                                                                                     |
| ---------------------------------------------- | ---------------------------------------------------------------------------------------- |
| `apps/web/src/pages/BusinessWorkspacePage.vue` | 增加商机看板、商机分栏详情、V2报备步骤表单、V2智能报价配置器、手机路由返回和产品选择状态 |
| `apps/web/src/styles/theme.css`                | 增加商机看板、商机分栏弹窗、报备步骤条、报价配置器、移动列表容器和小屏适配样式           |
| `apps/web/src/router/index.ts`                 | 增加手机新建报价、手机产品目录路由                                                       |
| `apps/web/index.html`                          | 补齐移动端安全区和手机网页元信息                                                         |
| `apps/web/e2e/layout.spec.ts`                  | 增加模块化 mock 数据和 V2 严格还原专项断言                                               |

## 3. V2对照依据

| V2文件                                                                  | 对照内容                                     |
| ----------------------------------------------------------------------- | -------------------------------------------- |
| `frontend/partner-app.js`                                               | 商机列表/看板、商机详情分栏、新建报价配置器  |
| `frontend/admin-app.js`                                                 | 管理端业务列表、报价预览、审核和后台体感     |
| `frontend/style.css`                                                    | 表格、卡片、弹窗、报价器、看板列基础风格     |
| `frontend/mobile.html`、`frontend/mobile.css`、`frontend/mobile-app.js` | 手机头部、底部导航、列表卡片和二级页返回体感 |

## 4. 验收记录

执行环境：

```bash
Node.js v22.13.1
npm 10.9.2
```

已执行命令：

```bash
PATH="$PWD/.cache/nodejs/node-v22.13.1-darwin-arm64/bin:$PATH" npm --workspace @lianruan/web run check
PATH="$PWD/.cache/nodejs/node-v22.13.1-darwin-arm64/bin:$PATH" npm --workspace @lianruan/web run test:e2e
PATH="$PWD/.cache/nodejs/node-v22.13.1-darwin-arm64/bin:$PATH" npm run verify
PATH="$PWD/.cache/nodejs/node-v22.13.1-darwin-arm64/bin:$PATH" npm run verify 2>&1 | tee tmp/verify-logs/stage9.12-verify-20260728.log
```

原始日志：

```text
tmp/verify-logs/stage9.12-verify-20260728.log
```

验证结果：

| 验证项                | 结果                                             |
| --------------------- | ------------------------------------------------ |
| Web类型检查           | 通过                                             |
| Web E2E               | 通过，13个业务场景，桌面和移动两个项目共26次执行 |
| 完整 `npm run verify` | 通过                                             |
| 格式检查              | 通过                                             |
| 全工作区构建          | 通过                                             |
| 类型检查              | 通过                                             |
| ESLint                | 0个错误，10个既有复杂度/函数长度警告             |
| 单元与接口测试        | 通过                                             |
| 浏览器入口测试        | 通过，13个业务场景，桌面和移动两个项目共26次执行 |
| 生产构建              | 通过                                             |
| 构建产物敏感信息扫描  | 通过                                             |

说明：

1. 构建过程中存在 Vite 大 chunk 警告，属于既有前端拆包优化提醒，不影响本次通过。
2. ESLint 输出为 warning，不阻断 verify；其中多数来自阶段9既有业务兼容层复杂度。
3. `scripts/verify.mjs` 末尾输出“阶段1完整验证通过”，这是现有脚本沿用早期阶段命名；本次实际执行内容覆盖阶段9.12所需的格式、构建、类型、单元、接口、浏览器和产物扫描。
4. E2E 使用前端接口桩数据验证页面结构、路由和交互存在性；真实后端数据、权限边界和外部接口兼容仍需 HexHub 远端联调验收。

## 5. 新增自动化验收点

| 用例                                 | 验收目标                                               |
| ------------------------------------ | ------------------------------------------------------ |
| 商机管理展示V2列表看板和分栏详情     | 列表、看板、阶段列、详情分栏必须存在                   |
| 新建报备展示V2步骤条和企业查询提示   | 报备页不能退回通用两列表单；企业查询为提示和入口保留   |
| 新建报价展示V2智能报价配置器         | 必须展示三段式报价器、模式卡片、产品卡片和报价汇总     |
| 手机列表写入入口保持移动端路由       | 手机写入入口必须跳 `/mobile/partner/registrations/new` |
| 手机管理员业务查询展示V2四段切换     | 管理员手机业务查询保持报备、商机、报价、订单四段       |
| 新建手机页隐藏底部导航并显示返回按钮 | 手机二级页不显示底部导航                               |

## 6. 截图留存

截图目录：

```text
output/playwright/stage9.12-v2-strict-correction
```

本轮有效截图：

| 文件                          | 页面             |
| ----------------------------- | ---------------- |
| `01-V2默认厂商登录卡.png`     | 默认厂商登录卡   |
| `02-V2桌面渠道工作台.png`     | 渠道桌面工作台   |
| `03-V2商机列表视图.png`       | 商机列表视图     |
| `04-V2商机看板视图.png`       | 商机看板视图     |
| `05-V2商机分栏详情.png`       | 商机左右分栏详情 |
| `06-V2新建报备步骤条.png`     | 新建报备步骤条   |
| `07-V2新建报价智能配置器.png` | 智能报价配置器   |
| `08-V2产品目录层级.png`       | 产品目录层级     |
| `09-V2手机渠道首页.png`       | 手机渠道首页     |
| `10-V2手机列表写入入口.png`   | 手机列表写入入口 |
| `11-V2手机我的页.png`         | 手机我的页       |
| `12-V2手机新建报价.png`       | 手机新建报价     |

说明：目录内 `旧截图-待忽略/` 为阶段9.12早期重拍前的旧截图隔离目录，不作为本轮最终验收依据。

## 7. 当前仍需说明的边界

| 项目             | 当前状态                                                       | 建议                                         |
| ---------------- | -------------------------------------------------------------- | -------------------------------------------- |
| HexHub远端       | 已发布到 HexHub「渠道CRM（新架构）」，版本 `3.0.0-stage9.12.20260728` | 已完成健康检查、12页截图巡检和主链路验收 |
| 逐页像素级复刻   | 已完成关键业务页结构纠偏，但仍不是逐像素复制 V2                | 后续按用户截图继续逐页微调                   |
| 商机阶段推进     | 已恢复阶段入口和视觉结构，真实阶段流转仍调用现有跟进接口或留口 | 后续补独立阶段推进接口                       |
| 报价复杂产品规则 | 已恢复 V2 配置器体感，复杂套餐价格规则仍按现有后端试算能力     | 后续补完整套餐、补充功能、硬件数量和折扣规则 |
| 企业查询回填     | 已恢复 V2 入口提示，不等于正式工商查询和重复校验能力           | 后续接入真实企业查询、回填和重复校验接口     |
| 手机详情页       | 本轮保证移动路由和二级页体感，详情仍可继续从弹窗改独立详情页   | 后续按 V2 手机详情页专项处理                 |
| 导入正式入库     | 本轮未改导入能力                                               | 后续接 Redis 队列、错误行下载、任务历史      |

## 8. HexHub远端发布验收

访问地址：

```text
http://10.20.3.10/login
```

远端版本：

```text
3.0.0-stage9.12.20260728
```

远端部署日志：

```text
/root/v3-stage9-12-hexhub-deploy-20260728-135350.log
```

远端页面巡检：

```text
output/playwright/hexhub-stage9-12/页面巡检结果.json
```

结果：12个页面全部通过，控制台异常0，页面异常0。

远端业务闭环：

```text
output/hexhub-stage9-12/业务闭环验收-2026-07-28T060209628Z.json
```

结果：管理员登录、客户报备、报备审核、商机、报价试算、报价、报价审核、转订单、订单一级确认全部通过。

## 9. 交付建议

下一步建议让用户在 HexHub「渠道CRM（新架构）」测试资产上做页面登录、V2 UI体感和主业务链路人工验收。

远端验收顺序：

1. 打开 `/login`，确认默认厂商登录卡和入口切换。
2. 登录渠道端，检查工作台、商机管理、客户报备、报价管理、订单管理、产品目录。
3. 在商机管理切换看板，打开商机详情，确认左右分栏。
4. 新建客户报备，确认步骤条和客户信息面板。
5. 新建报价单，确认智能报价配置器三段式。
6. 手机访问 `/mobile/partner/home`，检查底部导航、客户报备列表、新建页返回。
7. 按报备客户、报备商机、报价、订单闭环做业务验收。
