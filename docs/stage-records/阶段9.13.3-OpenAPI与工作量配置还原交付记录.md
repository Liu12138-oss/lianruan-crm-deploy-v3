# 阶段9.13.3 OpenAPI 与工作量配置还原交付记录

## 交付目的

阶段9.13.3 的目标是把 V2 已有 OpenAPI 对接能力和工作量配置能力迁移到 V3 PostgreSQL 正式表，并在 V3 后台提供可维护页面。报价试算和保存报价必须读取正式工作量配置，不能写死在前端或内存服务里。

## 需求确认

1. 工作量配置必须从 V2 映射到 PostgreSQL。
2. V3 后台必须能维护工作量映射。
3. 维护后的工作量规则必须影响报价工作量试算。
4. OpenAPI Client、密钥、权限和调用日志都必须走 PostgreSQL 正式表。
5. 测试和生产都必须按 PostgreSQL 正式表验收。

## 数据库交付

新增迁移：

```text
/Users/liu/Documents/Codex/lianruan-crm-deploy-v3/database/migrations/20260728_S9_013_工作量映射维护.sql
```

关键变化：

1. `catalog.workload_mappings` 增加 `product_ref_type`、`product_ref_id`、`delivery_tags`、`extra_json`。
2. 软件功能映射继续兼容原 `feature_id`。
3. 硬件产品映射通过 `product_ref_type = 'hardware'` 和 `product_ref_id` 落表。
4. 优先使用 V2 `deliveryTags`，缺失时再用原分类编码补齐。
5. 已解决的 `S8_4_WORKLOAD_FEATURE_MISSING` 迁移错误会在迁移中清理。

## 后端交付

主要文件：

```text
/Users/liu/Documents/Codex/lianruan-crm-deploy-v3/apps/api/src/business-store.ts
/Users/liu/Documents/Codex/lianruan-crm-deploy-v3/apps/api/src/business-routes.ts
```

已接入接口：

1. `GET /api/open-api/overview`
2. `GET /api/open-api/docs`
3. `GET /api/open-api/docs/:id/download`
4. `GET /api/open-api/clients`
5. `POST /api/open-api/clients`
6. `PUT /api/open-api/clients/:id`
7. `POST /api/open-api/clients/:id/reset-secret`
8. `GET /api/open-api/logs`
9. `POST /api/open/v1/auth/token`
10. `GET /api/open/v1/auth/me`
11. `GET /api/open/v1/:module`
12. `GET /api/workload/mappings`
13. `PUT /api/workload/mappings/:id`
14. `GET /api/workload/delivery-rules`
15. `PUT /api/workload/delivery-rules/:id`

已验证能力：

1. OpenAPI Client 可新增、编辑、重置密钥。
2. AppSecret 只在创建或重置时返回一次。
3. 开放接口令牌按 AppKey、AppSecret、状态、白名单和资源权限校验。
4. 开放接口调用写入 PostgreSQL 审计日志。
5. 工作量映射可查询和保存。
6. 交付工作量规则可查询和保存。
7. 报价试算读取 PostgreSQL 工作量映射和交付规则。

## 前端交付

主要文件：

```text
/Users/liu/Documents/Codex/lianruan-crm-deploy-v3/apps/web/src/pages/BusinessWorkspacePage.vue
/Users/liu/Documents/Codex/lianruan-crm-deploy-v3/apps/web/src/api/business-client.ts
/Users/liu/Documents/Codex/lianruan-crm-deploy-v3/apps/web/src/styles/theme.css
```

后台专页：

1. `/admin/openapi-integration`：OpenAPI 总览、对接参数、文档下载、Client 凭证管理、调用审计、新增编辑弹窗、密钥一次性展示弹窗。
2. `/admin/workload-config`：标准工作量配置、产品映射规则、新版交付工作量规则、规则编辑弹窗。

## 验收结果

执行命令：

```bash
PATH="$PWD/.cache/nodejs/node-v22.13.1-darwin-arm64/bin:$PATH" npm run verify
```

结果：通过。

重点通过用例：

1. 阶段8 V2 数据 1091 条装载到 PostgreSQL 并通过数量守恒。
2. 报备、审核、商机、报价试算、报价、订单确认链路通过。
3. OpenAPI Client 维护、密钥重置、Token 签发、开放资源读取、调用日志入库通过。
4. 工作量映射维护、交付规则维护、报价试算受规则影响通过。
5. 管理端 OpenAPI 专页和工作量配置专页浏览器验收通过。

## 注意事项

1. 测试、验收、生产必须使用 PostgreSQL 正式表。
2. 内存服务只作为开发兼容，不允许作为测试通过依据。
3. 工作量标签当前按 V2 既有产品类型映射，后续新增产品时需在后台工作量配置页维护映射。
4. OpenAPI AppSecret 不可反查，创建或重置后必须由管理员当场保存。
5. 后续如扩展异机备份、域名证书、集群部署，不需要改业务模型，但需要补部署层配置。
