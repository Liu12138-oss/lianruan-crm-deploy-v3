# 阶段5前端工程化迁移准备记录

生成时间：2026-07-23

## 1. 工作范围

本次只在 worktree `/Users/liu/Documents/Codex/lianruan-crm-deploy-v3-worktrees/phase-5-frontend-migration` 内工作，分支为 `phase/5-frontend-migration`。

本次未修改 V3 主目录，未触碰 V2 目录，未修改历史前端业务页面，未新增业务功能，未重做视觉。

## 2. 启动核对

启动前已执行：

```text
git status --short --branch
```

结果：

```text
## phase/5-frontend-migration
```

## 3. 已完成资产

| 文件 | 说明 |
| --- | --- |
| `docs/phase-5-frontend-migration/阶段5前端工程化迁移落地包.md` | 四端页面、路由、状态、接口依赖迁移切片和五个批次 |
| `docs/phase-5-frontend-migration/旧前端组件拆分边界建议.md` | `admin-app.js`、`partner-app.js`、`mobile-app.js`、`mobile-v2-app.js` 组件拆分边界 |
| `docs/phase-5-frontend-migration/API客户端分层与异常页面规范.md` | 新 API 客户端分层、错误模型、会话失效页、无权限页、维护页要求 |
| `docs/phase-5-frontend-migration/迁移验收用例与回退策略.md` | 每批迁移验收用例、通用验收矩阵、页面级和批次级回退策略 |
| `tests/phase-5-frontend-migration/阶段5前端迁移资产检查.js` | 检查迁移资产齐全，并确认旧前端业务文件未被修改 |

## 4. 验收记录

已执行：

```text
node tests/phase-5-frontend-migration/阶段5前端迁移资产检查.js
```

结果：

```text
阶段5前端迁移资产检查通过：文档齐全，历史前端业务文件未改动。
```

已执行阶段0静态校验：

```text
node tests/legacy-contract/接口基线一致性检查.js
node tests/legacy-browser/浏览器入口静态检查.js
```

结果：

```text
接口契约校验通过：257 个接口与基线一致。
浏览器入口静态检查通过：6 个入口已检查。
```

说明：阶段0静态脚本会刷新 `docs/baseline` 下的时间戳，本次校验后已恢复时间戳，避免把阶段0报告作为阶段5变更提交。

## 5. 当前限制

- 阶段1前端工程骨架未合入，本次不创建正式 `apps/web` 业务实现。
- 阶段3会话、权限、工作空间接口未定稿，本次只定义前端对接要求。
- 阶段4领域接口未稳定，本次不改旧页面调用和业务行为。
- 移动端二期已有写契约保护思想，迁移时必须继续保留“服务端未发布写契约则不发送请求”的规则。

## 6. 交接建议

1. 阶段1合入后，先按落地包创建目标目录、路由框架、统一客户端和错误页面。
2. 阶段3确认会话和权限接口后，再实现路由守卫和工作空间导航。
3. 阶段4每稳定一个领域接口，就按批次迁移对应页面。
4. 每个页面迁移前先补旧页面截图和浏览器基线，迁移后按验收矩阵跑用例。
5. 页面上线必须配置页面级路由开关和回退策略。
