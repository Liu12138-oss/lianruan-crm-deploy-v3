# 阶段4高风险接口清单

生成时间：2026-07-23T12:06:13.818Z

## 风险统计

| 风险类别 | 接口数 | 阶段4处理要求 |
| --- | ---: | --- |
| 信任前端身份 | 90 | 从阶段3服务端会话取得操作者、角色、区域、渠道和数据范围。 |
| 无分页 | 62 | 列表默认分页，最大页大小由服务端限制。 |
| 内存全量扫描 | 174 | 改为仓储层索引查询、聚合查询或版本化缓存。 |
| 同步导入导出 | 6 | 导入导出分为预检、任务、文件结果和审计，不在请求中执行大任务。 |
| 敏感字段返回 | 12 | 响应字段白名单，密码、密钥和令牌按场景脱敏或一次性返回。 |

## 接口明细

| 接口编号 | 方法 | 旧路径 | 新模块 | 风险类别 | 证据 | 处理要求 |
| --- | --- | --- | --- | --- | --- | --- |
| API-0002 | GET | /api/mock/crm-open-api/v1/identity/users/:userId | 开放接口 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0003 | POST | /api/auth/login | 认证 | 信任前端身份 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0004 | PUT | /api/auth/password | 认证 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0005 | GET | /api/auth/me | 认证 | 敏感字段返回 | 响应片段可能返回密码、密钥、令牌或包含原始用户对象 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0006 | GET | /api/notifications | 开放接口 | 信任前端身份、无分页、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0007 | PUT | /api/notifications/read | 开放接口 | 信任前端身份 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0008 | POST | /api/sso/iam/admin-login | 认证 | 信任前端身份、敏感字段返回 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；响应片段可能返回密码、密钥、令牌或包含原始用户对象 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0009 | POST | /api/sso/iam/admin-login-v2 | 认证 | 信任前端身份、敏感字段返回 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；响应片段可能返回密码、密钥、令牌或包含原始用户对象 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0010 | POST | /api/sso/iam/partner-login-v2 | 认证 | 信任前端身份、敏感字段返回 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；响应片段可能返回密码、密钥、令牌或包含原始用户对象 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0011 | POST | /api/registrations | 报备 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0012 | GET | /api/registrations | 报备 | 信任前端身份、无分页、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0013 | PUT | /api/registrations/:id/status | 报备 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0014 | PUT | /api/registrations/:id | 报备 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0015 | DELETE | /api/registrations/:id | 报备 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0016 | POST | /api/opportunities | 商机 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0017 | GET | /api/opportunities | 商机 | 信任前端身份、无分页、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0018 | PUT | /api/opportunities/:id | 商机 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0020 | GET | /api/audit-logs/:id | 审计 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0023 | PUT | /api/workload/delivery-rules/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0024 | GET | /api/workload/mappings | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0025 | PUT | /api/workload/mappings/:featureId | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0026 | GET | /api/workload/rules | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0028 | PUT | /api/workload/rules/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0029 | DELETE | /api/workload/rules/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0031 | POST | /api/ipg/quote-preview | 报价 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0032 | POST | /api/quotes | 报价 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0033 | GET | /api/quotes | 报价 | 信任前端身份、无分页、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0034 | DELETE | /api/quotes/:id | 报价 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0035 | POST | /api/orders | 订单 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0036 | GET | /api/orders | 订单 | 信任前端身份、无分页、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0037 | DELETE | /api/orders/:id | 订单 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0038 | PUT | /api/quotes/:id | 报价 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0039 | PUT | /api/quotes/:id/status | 报价 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0040 | PUT | /api/orders/:id/status | 订单 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0041 | PUT | /api/orders/:id/price-adjust | 订单 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0042 | GET | /api/users | 账号 | 信任前端身份、无分页、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0043 | PUT | /api/users/:id | 账号 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0044 | DELETE | /api/users/:id | 账号 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0045 | PUT | /api/users/:id/password | 账号 | 信任前端身份 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0046 | POST | /api/users/sync-status | 账号 | 信任前端身份、内存全量扫描、敏感字段返回 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合；响应片段可能返回密码、密钥、令牌或包含原始用户对象 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0051 | POST | /api/mobile/v2/registration-drafts | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0053 | POST | /api/mobile/v2/registrations | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0054 | PUT | /api/mobile/v2/registrations/:id/review | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0055 | POST | /api/mobile/v2/registrations/:id/copy | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0056 | GET | /api/mobile/v2/registrations/:id/events | 移动端 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0057 | GET | /api/mobile/v2/registrations | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0058 | GET | /api/mobile/v2/registrations/:id | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0059 | GET | /api/mobile/v2/opportunities | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0060 | GET | /api/mobile/v2/opportunities/:id | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0061 | POST | /api/mobile/v2/opportunities | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0062 | PUT | /api/mobile/v2/opportunities/:id | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0063 | PUT | /api/mobile/v2/opportunities/:id/stage | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0064 | POST | /api/mobile/v2/opportunities/:id/follow-ups | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0065 | GET | /api/mobile/v2/opportunities/:id/events | 移动端 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0066 | GET | /api/mobile/v2/quotes | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0067 | GET | /api/mobile/v2/quotes/:id | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0068 | GET | /api/mobile/v2/orders | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0069 | GET | /api/mobile/v2/orders/parent-options | 移动端 | 信任前端身份、无分页、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0070 | GET | /api/mobile/v2/orders/:id | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0071 | GET | /api/mobile/v2/quote-products | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0073 | POST | /api/mobile/v2/quote-drafts | 移动端 | 信任前端身份 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0074 | POST | /api/mobile/v2/quotes | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0075 | PUT | /api/mobile/v2/quotes/:id/discount-review | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0076 | GET | /api/mobile/v2/quotes/:id/discount-review | 移动端 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0077 | PUT | /api/mobile/v2/quotes/:id/confirm | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0078 | POST | /api/mobile/v2/order-drafts | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0079 | POST | /api/mobile/v2/orders/:id/submit | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0080 | PUT | /api/mobile/v2/orders/:id/primary-review | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0081 | PUT | /api/mobile/v2/orders/:id/fulfillment | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0082 | ALL | /api/mobile/v2/orders/:id/price-adjustments | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0083 | ALL | /api/mobile/v2/orders/:id/adjust-price | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0084 | ALL | /api/mobile/v2/orders/:id/cancel | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0085 | ALL | /api/mobile/v2/orders/:id/refund | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0086 | ALL | /api/mobile/v2/orders/:id/refunds | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0087 | GET | /api/mobile/v2/quotes/:id/events | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0088 | GET | /api/mobile/v2/orders/:id/events | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0089 | GET | /api/mobile/v2/partners | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0090 | GET | /api/mobile/v2/partners/:id | 移动端 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0093 | GET | /api/mobile/registrations | 移动端 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0095 | GET | /api/mobile/registrations/:id | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0096 | POST | /api/mobile/registrations | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0097 | GET | /api/mobile/opportunities | 移动端 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0098 | GET | /api/mobile/opportunities/:id | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0099 | POST | /api/mobile/opportunities/:id/follow-ups | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0100 | POST | /api/mobile/opportunities | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0101 | GET | /api/mobile/quotes | 移动端 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0102 | GET | /api/mobile/quotes/:id | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0103 | POST | /api/mobile/quotes/:id/convert-order | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0104 | GET | /api/mobile/orders | 移动端 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0105 | GET | /api/mobile/orders/:id | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0106 | GET | /api/mobile/partners | 移动端 | 信任前端身份、无分页、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0107 | GET | /api/mobile/partners/:id | 移动端 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0108 | PUT | /api/mobile/registrations/:id/status | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0109 | GET | /api/mobile/pending-approvals | 移动端 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0110 | PUT | /api/mobile/partners/:id/status | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0111 | POST | /api/mobile/logout | 移动端 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0112 | PUT | /api/mobile/pending-approvals/:id | 移动端 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0113 | GET | /api/channel-operations/overview | 渠道 | 信任前端身份 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0114 | GET | /api/channel-targets | 渠道 | 信任前端身份、无分页、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0115 | POST | /api/channel-targets | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0116 | PUT | /api/channel-targets/:id | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0117 | GET | /api/channel-visits | 渠道 | 信任前端身份、无分页、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0118 | POST | /api/channel-visits | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0119 | PUT | /api/channel-visits/:id | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0120 | POST | /api/admin/accounts | 账号 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0121 | POST | /api/users | 账号 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0123 | GET | /api/partners | 渠道 | 信任前端身份、无分页、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0124 | POST | /api/partners | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0125 | GET | /api/partner-profile-products | 渠道 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0126 | POST | /api/partner-profile-products | 渠道 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0127 | PUT | /api/partner-profile-products/:id | 渠道 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0128 | DELETE | /api/partner-profile-products/:id | 渠道 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0129 | GET | /api/partners/:id/profile | 渠道 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0130 | PUT | /api/partners/:id/profile | 渠道 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0131 | GET | /api/partners/:id | 渠道 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0132 | PUT | /api/partners/:id/status | 渠道 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0133 | POST | /api/partners/:id/staff | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0134 | PUT | /api/partners/:partnerId/staff/:staffId | 渠道 | 信任前端身份 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0135 | PUT | /api/partners/:partnerId/staff/:staffId/status | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0136 | PUT | /api/partners/:partnerId/staff/:staffId/password | 渠道 | 信任前端身份 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0137 | DELETE | /api/partners/:partnerId/staff/:staffId | 渠道 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0138 | PUT | /api/users/:id/status | 账号 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0139 | PUT | /api/partners/:id | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0140 | DELETE | /api/partners/:id | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0141 | PUT | /api/partners/:id/level | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0142 | PUT | /api/partners/:id/unbind | 渠道 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0143 | PUT | /api/partners/:id/add-parent | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0144 | PUT | /api/partners/:id/rebind | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0145 | POST | /api/partners/secondary/apply | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0146 | GET | /api/partners/secondary/pending | 渠道 | 信任前端身份、无分页、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0147 | PUT | /api/partners/secondary/:id/approve | 渠道 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0148 | PUT | /api/partners/secondary/:id/reject | 渠道 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0149 | PUT | /api/orders/:id/primary-confirm | 订单 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0150 | PUT | /api/orders/:id/primary-reject | 订单 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0151 | GET | /api/pending-approvals | 审批 | 信任前端身份、无分页、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0152 | PUT | /api/pending-approvals/:id | 审批 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0153 | GET | /api/dashboard/stats | 渠道 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0154 | GET | /api/product-tree | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0155 | GET | /api/categories | 产品 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0157 | PUT | /api/categories/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0158 | DELETE | /api/categories/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0159 | GET | /api/modules | 产品 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0160 | POST | /api/modules | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0161 | PUT | /api/modules/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0162 | DELETE | /api/modules/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0163 | GET | /api/features | 产品 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0164 | POST | /api/features | 产品 | 信任前端身份 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0165 | PUT | /api/features/:id | 产品 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0166 | DELETE | /api/features/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0167 | PATCH | /api/features/:id/publish | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0168 | GET | /api/hardware | 产品 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0170 | PUT | /api/hardware/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0171 | DELETE | /api/hardware/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0172 | PATCH | /api/hardware/:id/publish | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0173 | GET | /api/packages | 产品 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0174 | GET | /api/packages/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0176 | PUT | /api/packages/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0177 | DELETE | /api/packages/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0178 | PATCH | /api/packages/:id/publish | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0179 | GET | /api/products | 产品 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0180 | GET | /api/products/stats | 产品 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0181 | GET | /api/products/:id | 产品 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0183 | PUT | /api/products/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0184 | DELETE | /api/products/:id | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0185 | PATCH | /api/products/:id/publish | 产品 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0186 | POST | /api/products/batch-publish | 产品 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0189 | GET | /api/oauth/callback | 认证 | 信任前端身份 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0190 | GET | /api/oauth/userinfo | 认证 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0191 | POST | /api/oauth/logout | 认证 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0192 | GET | /api/company-search | 开放接口 | 信任前端身份、内存全量扫描、同步导入导出 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合；涉及文件读取、表格解析、导出生成或同步日志文件读取 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0193 | GET | /api/export/:type | 导入导出 | 信任前端身份、同步导入导出 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；涉及文件读取、表格解析、导出生成或同步日志文件读取 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0194 | GET | /api/import/:type/template | 导入导出 | 同步导入导出 | 涉及文件读取、表格解析、导出生成或同步日志文件读取 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0195 | POST | /api/import/:type/preview | 导入导出 | 同步导入导出 | 涉及文件读取、表格解析、导出生成或同步日志文件读取 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0196 | POST | /api/import/:type/execute | 导入导出 | 信任前端身份、内存全量扫描、同步导入导出、敏感字段返回 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合；涉及文件读取、表格解析、导出生成或同步日志文件读取；响应片段可能返回密码、密钥、令牌或包含原始用户对象 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0198 | GET | /api/open/v1/${pathName}/:id | 开放接口 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0199 | GET | /api/open-api/overview | 开放接口 | 敏感字段返回 | 响应片段可能返回密码、密钥、令牌或包含原始用户对象 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0202 | GET | /api/open-api/clients | 开放接口 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0203 | POST | /api/open-api/clients | 开放接口 | 内存全量扫描、敏感字段返回 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合；响应片段可能返回密码、密钥、令牌或包含原始用户对象 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0204 | PUT | /api/open-api/clients/:id | 开放接口 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0205 | POST | /api/open-api/clients/:id/reset-secret | 开放接口 | 内存全量扫描、敏感字段返回 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合；响应片段可能返回密码、密钥、令牌或包含原始用户对象 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0206 | GET | /api/open-api/logs | 开放接口 | 同步导入导出 | 涉及文件读取、表格解析、导出生成或同步日志文件读取 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0207 | POST | /api/open/v1/auth/token | 开放接口 | 信任前端身份、内存全量扫描、敏感字段返回 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合；响应片段可能返回密码、密钥、令牌或包含原始用户对象 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0211 | GET | /api/open/v1/users | 开放接口 | 信任前端身份、内存全量扫描、敏感字段返回 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合；响应片段可能返回密码、密钥、令牌或包含原始用户对象 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0212 | GET | /api/open/v1/users/:id | 开放接口 | 无分页、内存全量扫描、敏感字段返回 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合；响应片段可能返回密码、密钥、令牌或包含原始用户对象 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0213 | GET | /api/open/v1/partners | 开放接口 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0214 | GET | /api/open/v1/partners/:id | 开放接口 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0215 | GET | /api/open/v1/registrations | 开放接口 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0216 | GET | /api/open/v1/registrations/:id | 开放接口 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0217 | GET | /api/open/v1/opportunities | 开放接口 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0218 | GET | /api/open/v1/opportunities/:id | 开放接口 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0219 | GET | /api/open/v1/quotes | 开放接口 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0220 | GET | /api/open/v1/quotes/:id | 开放接口 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0221 | GET | /api/open/v1/orders | 开放接口 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0222 | GET | /api/open/v1/orders/:id | 开放接口 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0223 | GET | /api/open/v1/identity/users/:userId | 开放接口 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0229 | GET | /api/open/v1/analytics/partners/profile | 开放接口 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0235 | GET | /api/open/v1/analytics/regions/contribution | 开放接口 | 信任前端身份 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0236 | GET | /api/open/v1/analytics/owners/contribution | 开放接口 | 信任前端身份、内存全量扫描 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0237 | GET | /api/open/v1/categories | 开放接口 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0238 | GET | /api/open/v1/categories/:id | 开放接口 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0239 | GET | /api/open/v1/modules | 开放接口 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0240 | GET | /api/open/v1/modules/:id | 开放接口 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0241 | GET | /api/open/v1/features | 开放接口 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0242 | GET | /api/open/v1/features/:id | 开放接口 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0243 | GET | /api/open/v1/hardware | 开放接口 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0244 | GET | /api/open/v1/hardware/:id | 开放接口 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0245 | GET | /api/open/v1/packages | 开放接口 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0246 | GET | /api/open/v1/packages/:id | 开放接口 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0247 | GET | /api/open/v1/products | 开放接口 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0248 | GET | /api/open/v1/products/:id | 开放接口 | 无分页、内存全量扫描 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数；路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0253 | GET | /api/open/v1/audit-logs | 开放接口 | 信任前端身份 | 从请求体、查询参数或请求头读取操作者、角色、区域或渠道范围 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0254 | GET | /api/open/v1/audit-logs/:id | 开放接口 | 无分页 | 阶段0标记或路由片段未发现强制分页、偏移量或最大条数 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |
| API-0257 | POST | /api/open/v1/ipg/quote-preview | 开放接口 | 内存全量扫描 | 路由或适配处理依赖旧全量内存集合的查找、过滤、映射或聚合 | 迁移时必须先补契约用例，再接入服务端鉴权、分页、仓储查询、任务化或脱敏策略。 |

