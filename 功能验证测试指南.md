# 功能验证测试指南

## 更新日期：2026-04-13

---

## 一、新增功能概述

本次更新新增了以下两个功能：

### 1. 密码修改功能
- **适用角色**：超级管理员、区域管理员、渠道商（员工/企业管理员）
- **功能说明**：用户可以自主修改自己的登录密码
- **访问入口**：
  - 管理员端：侧边栏底部，点击「修改密码」卡片
  - 渠道商端：侧边栏底部，点击「修改密码」卡片

### 2. 订单价格调整功能
- **适用角色**：超级管理员、区域管理员
- **功能说明**：区域管理员可以调整自己区域内订单的价格，并填写调整原因
- **权限控制**：
  - 超级管理员可以调整所有订单
  - 区域管理员只能调整自己区域内的订单
  - 员工（渠道商）不能调整价格

---

## 二、本地启动服务

### 步骤 1：启动后端服务

```powershell
# 进入后端目录
cd "d:\AI项目及FRP\第二版（20260412）\lianruan-crm-deploy-v2.2.0\backend"

# 安装依赖（首次运行需要）
npm install

# 启动服务
node server.js
```

后端服务启动成功后会显示：
```
🚀 服务器运行在 http://localhost:3000
```

### 步骤 2：启动前端服务

```powershell
# 新开一个PowerShell窗口，进入前端目录
cd "d:\AI项目及FRP\第二版（20260412）\lianruan-crm-deploy-v2.2.0"

# 启动Python静态文件服务器（Windows）
python -m http.server 8080 --directory frontend
```

前端服务启动成功后会显示：
```
Serving HTTP on http://0.0.0.0:8080
```

---

## 三、功能测试验证

### 测试 1：修改密码功能

#### 测试账号

| 角色 | 账号 | 密码 | 区域 |
|------|------|------|------|
| 超级管理员 | admin | 123456 | - |
| 区域管理员 | admin_ah | 123456 | 安徽区 |
| 渠道商员工 | liujg | 123456 | 北区（政府企业）|

#### 测试步骤

**管理员端测试：**

1. 打开浏览器访问：`http://localhost:8080/admin.html`
2. 使用超级管理员账号登录：`admin` / `123456`
3. 在侧边栏底部点击「修改密码」卡片
4. 验证弹窗显示正常，包含：
   - 当前密码输入框
   - 新密码输入框
   - 确认新密码输入框
5. 测试错误输入：
   - 不输入当前密码 → 提示「请输入当前密码」
   - 新密码少于6位 → 提示「新密码长度不能少于6位」
   - 两次新密码不一致 → 提示「两次输入的新密码不一致」
6. 测试正确修改：
   - 当前密码：`123456`
   - 新密码：`654321`
   - 确认密码：`654321`
   - 点击「确认修改」→ 提示「密码修改成功！」
7. 使用新密码重新登录验证

**渠道商端测试：**

1. 打开浏览器访问：`http://localhost:8080/partner.html`
2. 使用渠道商员工账号登录：`liujg` / `123456`
3. 在侧边栏底部点击「修改密码」卡片
4. 重复上述测试步骤验证功能正常

---

### 测试 2：订单价格调整功能

#### 测试前提

需要先创建一条测试订单。如果没有现有订单，请联系区域管理员创建一个测试订单。

#### 测试步骤

1. 使用区域管理员账号登录 admin.html（如：`admin_ah` / `123456`）
2. 点击左侧菜单「📦 订单管理」
3. 在订单列表中点击一条订单进入详情
4. 验证订单详情页面中显示「💰 价格调整」区域（黄色背景）
5. 点击「调整价格」按钮
6. 验证价格调整表单显示：
   - 当前价格显示
   - 调整后价格输入框
   - 调整原因文本框（必填）
7. 测试错误输入：
   - 不填写调整原因 → 提示「请填写价格调整原因」
   - 输入无效价格（如0或负数） → 提示「请输入有效的调整后价格」
8. 测试正确调整：
   - 调整后价格：`99999`
   - 调整原因：「客户量大，给予优惠」
   - 点击「确认调整」→ 提示「价格已从 ¥XX,XXX 调整为 ¥99,999」
9. 验证调整后：
   - 订单金额已更新
   - 价格调整记录已显示在历史记录中
10. 尝试用其他区域管理员账号（如 `admin_js` / `123456`）登录，验证：
    - 无法看到非本区域的订单的「价格调整」功能

---

### 测试 3：验证原有功能不受影响

在测试新功能的同时，验证以下原有功能正常：

- [ ] 登录/退出登录
- [ ] 侧边栏导航
- [ ] 通知中心
- [ ] 客户报备管理
- [ ] 商机管理
- [ ] 报价管理
- [ ] 订单状态更新（确认/发货/完成/取消）
- [ ] 渠道商管理
- [ ] 员工账号管理（创建/编辑/禁用/删除/重置密码）

---

## 四、常见问题排查

### 问题 1：修改密码提示「原密码错误」

**原因**：输入的当前密码不正确

**解决**：
- 确认使用的是当前登录账号的正确密码
- 如果忘记密码，需要超级管理员通过重置功能将密码恢复为 `123456`

### 问题 2：价格调整提示「无权操作其他区域的订单」

**原因**：当前账号是区域管理员，尝试调整非本区域的订单

**解决**：
- 只能调整属于自己区域的订单
- 超级管理员可以调整所有订单

### 问题 3：后端服务启动失败

**可能原因**：
- 端口 3000 被占用
- Node.js 版本不兼容

**解决**：
```powershell
# 查看端口占用
netstat -ano | findstr :3000

# 结束占用进程或修改 server.js 中的 PORT
```

---

## 五、技术说明

### API 接口

#### 修改密码 API
```
PUT /api/auth/password
Content-Type: application/json

{
  "userId": "A001",
  "oldPassword": "123456",
  "newPassword": "654321"
}

Response:
{
  "success": true,
  "message": "密码修改成功"
}
```

#### 订单价格调整 API
```
PUT /api/orders/:id/price-adjust
Content-Type: application/json

{
  "newAmount": 99999,
  "adjustmentReason": "客户量大，给予优惠",
  "operatorId": "A002",
  "operatorName": "安徽区管理员",
  "operatorRole": "admin"
}

Response:
{
  "success": true,
  "data": { ... },
  "message": "订单价格已从 ¥15,000 调整为 ¥99,999"
}
```

---

## 六、文件变更清单

| 文件路径 | 变更说明 |
|----------|----------|
| backend/server.js | 新增修改密码API、新增订单价格调整API |
| frontend/admin-app.js | 新增修改密码界面、新增订单价格调整功能 |
| frontend/partner-app.js | 新增修改密码界面 |

---

如有问题，请联系开发人员。
