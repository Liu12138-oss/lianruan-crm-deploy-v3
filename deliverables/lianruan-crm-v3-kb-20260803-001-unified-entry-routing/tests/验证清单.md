# 验证清单

## 自动检查

- `scripts/precheck.sh` 通过。
- `scripts/upgrade.sh` 执行完成。
- `scripts/verify.sh` 通过。
- 升级日志中显示升级前备份目录，业务文件备份为 `uploads.zip`、`exports.zip`。
- `docker inspect` 显示 `api-1`、`api-2`、`nginx` 均已使用目标镜像标签。
- `/health/live` 返回版本 `3.0.0-stage9.23.20260803-kb001`。
- `/`、`/login`、`/admin`、`/partner` 返回 V3 工程化页面，状态码为 200。
- `/api/auth/sso/iam/config` 返回成功，并包含 `QdCRMguanlyuan123` 与 `QdCRMkeduduan123`。
- `/admin.html`、`/partner.html` 仍可访问，用于兼容存量入口。

## 人工检查

- 未登录访问 `/` 时进入 V3 登录页。
- 管理员账号登录后，桌面端进入 `/admin/dashboard`。
- 渠道账号登录后，桌面端进入 `/partner/dashboard`。
- 移动端访问时，管理员进入 `/mobile/admin/home`，渠道账号进入 `/mobile/partner/home`。
- 带 `redirect` 参数登录时，只能跳转到当前账号允许访问的内部路径。
- IAM H5 管理员单点登录入口能打开并回到管理端。
- IAM H5 渠道端单点登录入口能打开并回到渠道端。
- 添加渠道商员工、创建企业管理员、删除渠道商员工后刷新不残留，这三个前序问题继续正常。
- 原报备、商机、报价、订单页面可正常打开。
