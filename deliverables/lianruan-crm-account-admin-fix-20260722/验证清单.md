# 验证清单

## 已完成的隔离演练

演练在独立临时项目副本中进行，使用独立后端端口和前端端口；未访问、未部署、未写入生产目录或生产数据库。

| 项目 | 结果 |
| --- | --- |
| 补丁文件校验和 | 通过 |
| 三个脚本与公共函数脚本语法校验 | 通过 |
| 后端与前端补丁脚本语法校验 | 通过 |
| 升级前数据库完整性校验 | 通过 |
| 自动备份三份代码文件与两份 SQLite 一致性快照 | 通过 |
| 停服、原子替换三个文件、启服、只读健康检查 | 通过 |
| 历史孤儿员工精确删除接口 | 通过 |
| 成功升级前后主业务库业务内容指纹 | 一致 |
| 成功升级前后审计库业务内容指纹 | 一致 |
| 一键还原后三份代码文件基线校验和 | 一致 |
| 一键还原前后两库业务内容指纹 | 一致 |
| 模拟新后端启动失败后的自动代码回退 | 通过 |
| 自动回退后三份代码文件基线校验和、两库完整性与业务内容指纹 | 一致 |

主业务库指纹仅比较 `entity_name`、`id`、`data_json`，不比较 SQLite 内部更新时间字段；这是因为现有服务接收停止信号时会执行自身的正常保存流程。审计库比较完整审计记录。两种比较均用于确认升级脚本没有引入业务数据变化。

## 生产执行前检查

```bash
cd /home/liulonghai/tmp/lianruan-crm-account-admin-fix-20260722
bash check.sh
```

预检必须显示“升级前基线校验通过，可以执行升级”。如显示校验和不一致、进程归属异常、端口被其他服务占用或备份空间不足，应停止执行并保留现场，不要强制覆盖。

## 生产执行后检查

```bash
curl -fsS http://127.0.0.1:3000/api/users
curl -fsSI http://127.0.0.1:8080/admin.html
sqlite3 /home/liulonghai/lianruan-crm-deploy-v2.2.0/backend/crm.db 'PRAGMA integrity_check;'
sqlite3 /home/liulonghai/lianruan-crm-deploy-v2.2.0/backend/audit.db 'PRAGMA integrity_check;'
```

两次完整性检查均应返回 `ok`。随后由业务人员在界面复核历史企业管理员和渠道商员工的编辑、启停、重置密码及删除操作。
