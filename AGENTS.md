# 项目协作规则

## 版本边界

本项目后续所有功能改动、缺陷修复、入口调整和部署配置，均明确针对 V3 工程化应用。

V3 应用主线为：

- 前端：`apps/web`
- 后端：`apps/api`
- 后台任务进程：`apps/worker`
- MCP受控只读协议服务：`apps/mcp`
- 共享包：`packages/*`
- 单机部署：`deploy/single-server`

旧版 V2 静态目录、旧后端、旧升级包、历史交付包、阶段性过程资产和旧启动脚本已从主线清理，不再维护，不再新增能力，不再作为新需求落点：

- `frontend`
- `backend`
- `deliverables`
- `docs/baseline`
- `docs/phase-5-frontend-migration`
- `docs/stage-records`
- `scripts/database`
- `scripts/phase4`
- `tests/phase3`
- `tests/phase4`
- `tests/phase-5-frontend-migration`
- `tests/phase7`
- 根目录旧版启动、升级、备份脚本中仅服务 V2 的内容

V3 迁移兼容资产必须保留，包括：

- `database/mapping`
- `scripts/migration`
- `apps/api/src/v2-compat-routes.ts`
- `apps/api/tests/v2-compat.test.ts`
- `tests/phase8`

如发现 V2 旧入口和 V3 入口规则冲突，以 V3 为准。不得为了兼容 V2 旧发布方式而削弱或绕开 V3 的登录、权限、路由和部署规则。

## 业务扩展实施基线

涉及组织架构、合同审批、收款、企微 OA、客户公海池、报备池、消息提醒、渠道运营、合作协议、框架协议、保证金、标品报价、技术团队、证书管理或 MCP 的需求，编码前必须先阅读：

- `docs/V3业务平台底座与业务扩展实施交付方案.md`
- `docs/V3 MCP受控只读能力开发实施与交付手册.md`

实施顺序默认为：先完成现有 V3 功能的鉴权、权限、数据范围、审计、状态机、幂等和任务化收口，再依次建设组织架构、合同与收款、企微 OA、公海与报备池、企微助手与应用提醒。

新功能必须遵守文档中的业务域边界、状态机、数据模型、接口契约、任务机制和验收门禁。业务规则尚未确认时不得在代码中静默假设；需要改变方案时，必须同步更新该文档和对应测试。

## 统一入口规则

对外发布优先只发布一个 V3 应用入口：站点根路径 `/`。

用户进入统一入口后，由 V3 登录态、账号权限路径和访问设备共同决定实际页面：

- 未登录：进入 `/login`
- 已登录管理员账号：桌面端必须进入 `/admin.html`，移动端必须进入 `/admin-mobile.html`
- 已登录渠道账号：桌面端必须进入 `/partner.html`，移动端必须进入 `/partner-mobile.html`
- 带有安全 `redirect` 参数时：仅当目标路径在当前账号允许访问范围内才跳转

以上四个页面位于 `apps/web/public`，属于 V3 当前正式业务页面，不等同于根目录历史 V2 的 `frontend` 页面。统一入口不得把用户导向 `/admin/dashboard`、`/partner/dashboard`、`/mobile/admin/home`、`/mobile/partner/home` 等工程化迁移中的未替换页面。

统一登录成功后必须同时建立 HttpOnly Cookie 会话和四套正式业务页面所需的本地页面会话，然后使用浏览器整页跳转，禁止使用 Vue Router 在单页应用内部跳转到上述 `.html` 页面。入口分流规则集中维护在 `apps/web/src/router/entry-target.ts`。任何人调整登录、单点登录、根路径、移动端落点、角色落点或认证响应时，必须先阅读并同步更新该文件、`apps/web/src/api/auth-client.ts` 及其测试。

## 升级包自检规则

制作 V3 升级包时，升级脚本、验证脚本和 `tests/smoke.sh` 的断言必须和本次真实业务设计保持一致，禁止复制旧包脚本后不复核断言。

特别是登录和单点登录相关升级：

- 自 KB-20260812-021-R2 起，PC IAM、PC UniSDP 与移动端 IAM 统一入口识别和统一会话返回，IAM 配置接口不再依赖废弃的 PC 独立开关，`pcEnabled` 字段和 `V3_AUTH_SSO_PC_DISABLED` 拦截已从代码中整体移除。接口 `/api/auth/sso/iam/config` 的正确断言是 `enabled:true`，且响应包含 `entries`、`requestIsaidByEntry`、`timeoutMs` 字段；不得再断言 `enabled:false`，也不得再断言已删除的 `pcEnabled` 字段。
- 自检脚本不得写死 Vite 构建后的哈希资源文件名，例如 `/assets/index-xxxx.js`、`SingleSignOnPage-xxxx.js`；必须从 `/login` 或当前入口页面动态读取实际资源路径，或检查稳定的入口文件。
- 自检脚本里的每个关键字必须先在本地构建产物或目标环境接口响应中验证存在，再写入升级包。
- 升级包交付前必须实际执行：外层 `.zip.sha256` 校验、`unzip` 解压、包内 `manifest/SHA256SUMS` 校验、所有脚本 `bash -n`、以及自检脚本关键断言的本地等价验证。
- 如果升级已完成但仅自检脚本误判失败，应优先出 R2 修正包并说明可直接执行 `verify.sh`，不得要求用户回退已经健康通过的服务。
- 升级脚本使用 `set -o pipefail` 时，禁止写 `docker compose config | grep -q ...` 这类会被管道提前关闭误判失败的校验；必须先把 `docker compose config` 输出保存到变量或临时文件，再对保存内容做匹配。

## 协作原则：理解与提问

本项目协作中，Codex 必须遵守以下行为准则，且优先级高于上述具体规则中任何隐含的"先动手"倾向：

1. 先理解，再动手。每个任务开始前，必须先复述并对齐用户的目标、范围、约束、验收方式、回退方式以及潜在风险。
2. 把握不足 98% 不动手。如果对用户意图、影响面或正确做法没有达到至少 98% 的把握，禁止执行任何写入、修改、构建、发布或外部副作用操作；禁止以"猜一猜试试看"的方式推进。
3. 不确定就提问。当关键信息缺失、术语有歧义、需求与现有规范冲突、或存在多种合理方案时，必须主动向用户提问澄清，而不是自行假设。
4. 承认用户更懂业务。用户对业务、流程、对外承诺和历史决策更专业；Codex 的判断不能压过用户的明确意图。若用户已经给出决定，即使 Codex 认为有更优方案，也必须先指出，再请用户确认，而不是直接覆盖。
5. 提问要具体。每次提问必须列出已知信息、未知信息、可选方案、推荐方案及其影响，便于用户做最小决策。
6. 高风险动作必须先获得明确授权。任何会改动数据库、生产配置、生产数据、对外接口、发布包或既有规范的动作，事先要得到用户或项目负责人的明确授权，并保留可回退步骤。
7. 可逆优先。在不违背以上原则的前提下，优先选择只读检查、可逆变更和局部修复，避免一次性大规模改动。

本章节为 v1 最小实现；后续如需细化（如填写清单、补充示例或接入自动化校验），须同步在本文件追加或修订，并在交付说明中注明。

## hgrill 方案澄清与交付规则

本仓库是已有的 V3 工程化应用。涉及功能新增、现有功能行为调整或缺陷修复时，优先使用压缩包中的自包含技能：

- `hgrill-feature`：功能新增、功能修改和交互调整。
- `hgrill-bug`：缺陷定位与修复。
- `hgrill`：仅用于独立的新项目立项或与本仓库无关的从零方案；不得用于本仓库的日常改动。

### 使用前提与优先级

1. hgrill 负责把需求、方案和验收口径问清楚；本文件及项目现有文档始终是更高优先级的工程约束。
2. 使用 `hgrill-feature` 或 `hgrill-bug` 前，先阅读本文件、`docs/CONTEXT.md`（如存在）和相关 `docs/adr/`；涉及业务扩展时还必须阅读本文件“业务扩展实施基线”列出的两份方案文档。
3. hgrill 不能绕过“先理解，再动手”“把握不足 98% 不动手”、高风险动作授权、V3 鉴权权限和数据安全规则。技能模板与本文件冲突时，以本文件和用户明确决定为准，并在决策日志中记录冲突及处理结果。
4. 事实由 Agent 通过代码、测试、配置和文档只读核查；只有业务选择、取舍和验收口径需要向用户提问。未达成共同理解并获得用户确认前，不得修改代码、数据库、生产配置、发布包或对外接口。

### 访谈与记录要求

1. 每次 hgrill 会话都必须完成预检、按决策树分轮提问、收尾检查和共识确认；用户明确要求跳过的部分才可跳过，并记录用户原话和原因。
2. 每轮问题须给出已知事实、未知决策、可选方案、推荐方案及影响；不得用静默假设代替提问。问题、决策、否决项、权衡和待确认项应按技能规定写入对应 `docs/tasks/<任务slug>/docs/DECISION-LOG.md`。
3. 术语沿用仓库 `docs/CONTEXT.md`；新术语先更新该术语表。需要长期保留且难以逆转、脱离上下文会令人困惑、经过真实权衡的决策，才升级为 ADR。

### 产物位置与编码入口

1. `hgrill-feature` 和 `hgrill-bug` 的产物必须全部位于 `docs/tasks/<任务slug>/`：包括 `DESIGN.md`、`TICKETS.md`、决策日志、任务级 ADR 和临时文件；不得覆盖根目录既有 `AGENTS.md` 或 `DESIGN.md`。
2. 只有用户确认共识后，才能根据任务目录中的 `DESIGN.md` 和 `TICKETS.md` 开始编码。实现按任务单编号顺序执行，完成一个就核对其验收标准。
3. 任务单应明确实现切片、阻塞关系、验收标准、回归范围、异常降级、兼容性和回滚方式；涉及接口、数据库、权限、状态机、幂等、审计或部署的改动，必须同步列出对应测试和验证命令。
4. `hgrill` 的自动产出规则要求生成根目录 `AGENTS.md`，与本仓库规则冲突；因此本仓库禁止按该规则覆盖或重写 `AGENTS.md`。如确需新项目流程，必须先获得用户明确授权，并改为在独立目录或任务目录中产出。

### 语言与交付说明

hgrill 产物、代码、注释、测试说明、报错解释和最终汇报全部使用标准简体中文；保留代码、命令、路径、接口字段和第三方专有名词的原文形式。交付时说明采用的技能版本、共识摘要、变更文件、验证结果、风险和回退步骤。
